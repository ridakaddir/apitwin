version: 2

project_name: apitwin

before:
  hooks:
    - go mod tidy

builds:
  - id: apitwin
    main: .
    binary: apitwin
    ldflags:
      - -s -w -X github.com/ridakaddir/apitwin/cmd.version={{.Version}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: apitwin
    name_template: "{{.ProjectName}}_{{.Os}}_{{.Arch}}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - llms.txt
      - examples/**

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch
