{
  "status": "Deploying",
  "tenantId": "{path.tenantId}",
  "environment": "{path.env}",
  "region": "{.region}",
  "createdAt": "{{now}}",
  "deploymentId": "{{uuid}}",
  "models": "{{ref:stubs/environments/{path.env}/models/}}",
  "tenantConfig": "{{ref:stubs/tenants/{path.tenantId}/configs/{path.env}.json}}",
  "metadata": {
    "creator": "{header.X-User-Id}",
    "version": "{.version}"
  }
}